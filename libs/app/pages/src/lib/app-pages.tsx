import React from "react";

/* eslint-disable-next-line */
export interface AppPagesProps {}

export function AppPages(props: AppPagesProps) {
  return (
    <div>
      <h1>Welcome to app-pages!</h1>
    </div>
  );
}

export default AppPages;
