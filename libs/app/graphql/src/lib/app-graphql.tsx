import React from "react";

/* eslint-disable-next-line */
export interface AppGraphqlProps {}

export function AppGraphql(props: AppGraphqlProps) {
  return (
    <div>
      <h1>Welcome to app-graphql!</h1>
    </div>
  );
}

export default AppGraphql;
